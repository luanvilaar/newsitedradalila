# Linting Blockers — Devops Report

**Status:** BLOCKERS IDENTIFIED - Requires @dev fixes before push

**Branch:** `chore/init-github`

**Total Issues:** 63 errors, 22 warnings

---

## Critical Errors (Must Fix)

### 1. TypeScript `any` Types (15 instances)
Files requiring explicit type annotations instead of `any`:
- `src/app/api/auth/route.ts` (1)
- `src/app/api/documents/route.ts` (1)
- `src/app/api/documents/upload/route.ts` (1)
- `src/app/api/patients/[id]/anamnesis/route.ts` (1)
- `src/app/api/patients/[id]/bioimpedance/route.ts` (1)
- `src/app/api/patients/[id]/documents/route.ts` (1)
- `src/app/api/patients/[id]/prescriptions/route.ts` (1)
- `src/app/api/patients/[id]/route.ts` (3)
- And more...

**Fix:** Replace `any` with proper types (use `Request`, `NextResponse`, `unknown`, etc.)

### 2. React Hooks Rules Violation
**File:** `src/components/bioimpedance/BodySegmentIllustration.tsx:160`
- `useMemo` is called conditionally
- React Hooks must be called unconditionally in the same order

**Fix:** Move useMemo outside the conditional

### 3. Impure Function During Render
**File:** `src/components/bioimpedance/BodySegmentIllustration.tsx:732`
- `Math.random()` called during component render
- Use `useRef` or `useState` to generate stable random values

**Fix:** Generate random value once in useEffect or useRef, reuse in render

### 4. CommonJS Imports (2 instances)
**File:** `src/app/api/patients/[id]/bioimpedance/extract-pdf/route.ts:262`
- `require()` style imports forbidden in ESM project

**Fix:** Convert to ES import: `import pdfParse from 'pdf-parse';`

---

## Warnings (Low Priority)

### Unused Variables (22 instances)
- `src/components/bioimpedance/BodySegmentIllustration.test.tsx` (2)
- `src/components/bioimpedance/BodySegmentIllustration.tsx` (1)
- Various script files

**Fix:** Remove unused variables or prefix with `_` if intentional

---

## Next Steps

1. @dev: Fix all 63 errors above
2. Run `npm run lint` to verify
3. Report back to @devops when ready
4. @devops will execute push workflow

---

**Generated by:** Gage (devops)
**Date:** 2026-03-04
**Command:** `*pre-push` quality gate
